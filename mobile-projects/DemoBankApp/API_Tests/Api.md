# API Tests – DemoBankApp

Validación de endpoints con Postman:
- Login → POST /api/login
- Cuentas → GET /api/accounts
- Transferencias → POST /api/transfer

Cada request incluye validación de:
- Código de respuesta (200, 400, 401).
- Payload correcto.
- Mensajes de error claros.
